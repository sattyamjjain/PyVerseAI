# Required ONLY for live extraction. NOT needed to reproduce the committed
# results: `make run-mock` and `make test` replay committed fixtures offline.
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# Optional overrides (defaults shown)
# ANTHROPIC_MODEL=claude-sonnet-5
# OPENAI_MODEL=gpt-5.6-terra
# EXTRACTION_EFFORT=medium
